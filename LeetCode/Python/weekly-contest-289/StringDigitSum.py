# ------------------------------------------------------
# SOLUTION # 1
# ------------------------------------------------------
def solution(s, k):
    while(len(s) > k):
        temp = ""
        for val in (s[i:i+k] for i in range(0, len(s), k)):
            temp += str(sum(map(int, val)))
        s = temp
    return s

print(solution("00000000", 3))
# ------------------------------------------------------
# SOLUTION # 2
# ------------------------------------------------------
