class Solution:
    def closestMeetingNode(self, edges: list[int], node1: int, node2: int) -> int:
        # Make proper graph from edges list
        graph = {}
        for i, edge in enumerate(edges):
            if 
        # Get all nodes reachable from both node 1 and node 2
        # Find minimum distance to a node reachable by them both
            # Minimum distance is the max of the distances to the closest reachable node